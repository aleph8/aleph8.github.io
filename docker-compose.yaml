volumes:
  node_modules:

services:
  astro:
    build: .
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=development
    # Use a dummy command if we want to just keep it alive initially
    # command: tail -f /dev/null
